import React from 'react'
import './Pad.css';
import Button from '../Button/Button';

const pad = (props) => {

    return (
      <div className='Pad'>
        <Button />
      </div>
    );
  }

export default pad;
